# TODO — Session du 13 avril 2026

## Contexte
Session du 12 avril : 8h+ de travail sur customisation Studio.
Le build MFE avec tutor-indigo a plante (probablement manque de RAM).
Le LMS fonctionne, le Studio fonctionne mais sans nos modifications visuelles.

## Etat actuel
- LMS : OK, theme mission-theme actif
- Studio : OK, fonctionne mais branding OpenEdX par defaut
- Plugin mission_mfe_branding : installe et actif, MFE_CONFIG correct
- Plugin tutor-indigo : installe mais image MFE pas rebuilded (build plante)
- API MFE retourne les bonnes valeurs (slogan, powered=false, footer links)
- Repos mobile crees : mission-app-android + mission-app-ios

## A faire demain

### Priorite 1 — Relancer le build MFE
Sur le VPS :
```
# Arreter le build plante
docker buildx stop

# Relancer avec moins de parallelisme pour economiser la RAM
tutor images build --no-cache mfe --build-arg BUILDKIT_INLINE_CACHE=1
```

Si ca plante encore (RAM insuffisante) :
```
# Option : augmenter le swap
fallocate -l 4G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile

# Puis relancer le build
tutor images build --no-cache mfe
```

Apres le build :
```
tutor config save
tutor local stop && tutor local start -d
```

Puis verifier :
- Ctrl+Shift+R sur Studio
- Le slogan "Donnez du sens a votre parcours!" doit apparaitre dans le footer
- Le texte "edX and Open edX" doit avoir disparu
- Le logo Mission doit etre visible

### Priorite 2 — Si le build MFE fonctionne
Verifier toutes les pages Studio listees dans CAHIER_DE_CHARGE_STUDIO.md
Prendre des screenshots avant/apres pour Zakia

### Priorite 3 — Travailler avec Chloe sur les maquettes
Brief design dans CAHIER_DE_CHARGE_STUDIO.md (Sprint 1 livrables)

### Priorite 4 — App mobile
Cloner mission-app-ios et mission-app-android
Configurer l'URL de l'instance OpenEdX
Tester sur simulateur

## Fichiers de reference
- PROCESS_DEPLOIEMENT.md — process de deploy (3 types)
- CAHIER_DE_CHARGE_STUDIO.md — cahier de charge complet (4 sprints)
- diagnostics/2026-04-12_audit-studio-deploy.md — audit complet
- diagnostics/2026-04-12_process-deploiement.md — process technique
- diagnostics/2026-04-12_plugins-tutor-guide.md — guide plugins Tutor
- tutor_plugins/mission_mfe_branding.py — plugin branding MFE

## Commandes utiles pour demain
```
# Se connecter au VPS
ssh root@academie.staging.missionformations.com

# Verifier l'etat des containers
docker ps --format "{{.Names}} {{.Status}}" | grep tutor

# Verifier les plugins actifs
tutor plugins list | grep -E "mission|indigo"

# Verifier le MFE_CONFIG actif
curl -s https://academie.staging.missionformations.com/api/mfe_config/v1 | python3 -m json.tool | grep -i "slogan\|powered\|logo"

# Logs si probleme
docker logs tutor_local-lms-1 --tail 30
docker logs tutor_local-cms-1 --tail 30
docker logs tutor_local-mfe-1 --tail 30
```
